# open_epub 모노레포 — pub workspace 루트 (S10.1, ADR-008)
# 이 파일은 패키지가 아니라 workspace 루트다. 실제 패키지는 packages/* 에 있다.
name: open_epub_workspace
publish_to: "none"

environment:
  sdk: ">=3.6.0 <4.0.0"

# 워크스페이스 멤버 (단일 루트 lockfile 공유)
workspace:
  - packages/open_epub
  - packages/open_epub_engine
