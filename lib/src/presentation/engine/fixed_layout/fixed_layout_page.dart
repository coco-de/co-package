// Presentation Engine — open_epub 1.0
// Status: SCAFFOLD STUB. Implementation deferred to Story S1.8.
// BDD: F3.2 (핀치 줌), F3.5 (page-spread-left/right)

import 'package:flutter/widgets.dart';

class FixedLayoutPage extends StatelessWidget {
  const FixedLayoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError('S1.8');
  }
}
