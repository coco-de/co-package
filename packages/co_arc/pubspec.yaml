name: co_arc
description: Cocode Actions Runner Cluster — self-hosted 러너 관리 TUI(coarc)와 등록 스크립트, ARC 설정, 재사용 워크플로우.
version: 0.1.0
publish_to: none
repository: https://github.com/coco-de/co-package/tree/main/packages/co_arc
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

executables:
  coarc: coarc

dependencies:
  characters: ^1.3.0
  dart_tui: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2
