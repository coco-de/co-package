# This file includes the root analysis_options.yaml
include: ../../analysis_options.yaml

# Module-specific overrides
analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.pb.dart"
    - "**/*.pbenum.dart"
    - "**/*.pbjson.dart"
    - "**/*.pbserver.dart"

linter:
  rules:
    constant_identifier_names: false
