include: package:lints/recommended.yaml

analyzer:
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    # public_member_api_docs — GA/E6 발행 전 문서화 패스로 유예.
    # lints/recommended 에 없는 opt-in 규칙. S10.3에서 api/domain/data 30여 파일을
    # flutter_lints 하에서 추출·이동했고, 이들 공개 멤버 전수 문서화는 이동 스토리 범위 밖.
    # 발행(publish_to:none 해제, E6)과 함께 재활성화 + 문서 보강 예정. (.pipeline backlog_minor)
    # - public_member_api_docs
