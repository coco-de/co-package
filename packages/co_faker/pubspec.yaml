name: co_faker
description: >-
  Pure Dart fake data generation for tests, prototypes, fixtures, and local
  development. Deterministic, multilingual, and extensible without a server.
version: 0.1.0
repository: https://github.com/coco-de/co-package/tree/main/packages/co_faker
resolution: workspace

environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
